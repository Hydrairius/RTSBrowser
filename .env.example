# Server (packages/server)
JWT_SECRET=change-me-in-production-use-long-random-string
PORT=3001
DATABASE_PATH=./data/rtsbrowser.db
CORS_ORIGIN=http://localhost:5173
# Dev-only: GET /api/dev/snapshot (auto-on when NODE_ENV is not production)
# ENABLE_DEV_API=1
