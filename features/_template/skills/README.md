# Skills — `<feature-name>`

Place Cursor agent skills here (`SKILL.md` files). Each skill should:

- State when to use it (trigger phrases / tasks)
- Reference authoritative files in `../data/`
- Point to scripts in `../tools/` when relevant

Start with one skill per non-trivial workflow (e.g. “tune gather rates”, “author a new unit def”).
