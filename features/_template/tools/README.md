# Tools — `<feature-name>`

Scripts, CLIs, and small utilities for this feature during planning and development.

Guidelines:

- Prefer Node/TypeScript or Python consistent with the future `src/` stack (decide globally before adding many tools).
- Document usage in this README.
- Do not store secrets here; use environment variables and `.env.example` at repo root when `src/` exists.
