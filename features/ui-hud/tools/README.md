# Tools — ui-hud

| Tool | Purpose |
|------|---------|
| Playwright `e2e/capture-marketing.spec.ts` | Full-screen PNGs + gameplay video → `docs/marketing/` (`capture:marketing`, `capture:marketing:video`, `capture:marketing:all`) |

Future: layout screenshot diff, screen transition validator against `data/screens.json`.
